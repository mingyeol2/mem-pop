# [PRD] 뇌리에 콱 박히는 AI 암기 튜터: 짤기억(Mem-Pop)

단일 화면 연상 암기 & 1초 퀴즈 웹 서비스 제품 요구사항 정의서 (PRD)

---

## 1. 목표와 성공 조건

### 1.1 목표
- 외우기 힘든 단어·개념·연도를 단 한 줄 입력하는 것만으로, AI가 **재치 있는 3줄 연상 스토리**와 **3지선다 1초 퀴즈**를 즉시 생성하여 즉각적인 암기 몰입을 돕는 초경량 단일 화면 웹 서비스 구현.

### 1.2 성공 조건
- **단일 기능 집중:** 로그인, 결제, 백엔드 DB 연동 없이 브라우저 클라이언트 환경에서 프롬프트 요청 및 렌더링이 완결될 것.
- **성능 및 반응성:** 유효한 키워드 입력 후 생성 요청 시 5초 이내에 연상 스토리와 1초 퀴즈가 화면에 렌더링될 것.
- **인터랙션 완료도:** 퀴즈 보기 선택 시 페이지 새로고침이나 화면 전환 없이 즉시 정답/오답 여부와 해설이 시각적으로 피드백될 것.

---

## 2. 사용자 스토리

- **User Story:** 
  > *"시험/자격증을 준비 중인 수험생으로서, 도저히 안 외워지는 단어나 개념을 입력창에 넣고 버튼을 누르면, 5초 안에 직관적인 3줄 암기 팁과 확인 퀴즈를 제공받아 바로 암기하고 테스트해보고 싶다."*

---

## 3. 화면 명세 (UI/UX)

단일 화면(Single Page) 구조로 상단부터 하단까지 한눈에 파악 가능한 레이아웃을 구성한다.
## 4. 기능 명세

### 4.1 인풋 및 유효성 검증
- 단일 텍스트 입력 필드(Placeholder: `"외우고 싶은 단어, 개념, 연도를 1개만 입력하세요. 예: 임진왜란 1592, mitigate"`) 제공.
- 최대 입력 글자 수: **공백 포함 30자 제한**.

### 4.2 AI 암기 콘텐츠 생성 요청
- '생성하기' 버튼 클릭 시 LLM API로 프롬프트 전송.
- **AI 출력 포맷 (JSON 형식 제어):**
  - `story`: 3줄로 구성된 연상 암기 텍스트
  - `quiz`:
    - `question`: 1초 확인 퀴즈 질문
    - `options`: 3개의 보기 텍스트 배열
    - `answer_index`: 정답 인덱스 (`0`, `1`, `2` 중 하나)
    - `explanation`: 한 줄 정답 해설

### 4.3 결과 렌더링 및 퀴즈 인터랙션
- 응답 수신 즉시 결과 카드가 부드럽게 노출됨.
- **3지선다 보기 클릭 시:**
  - 선택된 보기가 정답이면 초록색 하이라이트 + "정답입니다!" 문구 및 해설 노출.
  - 오답이면 선택한 보기는 빨간색, 정답 보기는 초록색으로 표시 + "아쉬워요!" 문구 및 해설 노출.
  - 보기를 한 번 클릭하면 다른 보기 클릭 불가(비활성화).
- **'다시 생성하기' 버튼:** 기존 입력값을 유지한 상태에서 즉시 AI API를 재호출하여 새로운 연상 스토리와 퀴즈를 갱신.

---

## 5. 예외 처리 (Exception Handling)

### 5.1 [빈 값/공백 입력 예외]
- **상황:** 입력 필드가 완전히 비어있거나, 스페이스(공백)만 입력된 상태.
- **처리 방식:**
  - '생성하기' 버튼은 비활성화(Disabled) 상태를 기본으로 유지.
  - 비어있는 상태에서 포커스 아웃되거나 클릭을 시도할 경우, 입력창 테두리가 빨간색으로 변경되며 하단에 빨간색 안내 문구 노출:
    > *"외우고 싶은 단어나 개념을 입력해 주세요."*

### 5.2 [다중 키워드/글자 수 초과 입력 예외]
- **상황:** 여러 단어를 나열(예: `사과, 배, 포도, 수박`)하거나 30자를 초과하여 입력한 경우.
- **처리 방식:**
  - 30자를 초과하는 입력은 브라우저 레벨에서 타이핑/붙여넣기를 즉각 차단(`maxlength="30"`).
  - 쉼표(`,`), 슬래시(`/`), 앤드(`&`) 등 다중 키워드 구분자가 2개 이상 포함된 경우, 입력창 하단에 경고 문구 표시:
    > *"한 번에 한 가지 개념만 입력할 때 암기 효과가 가장 높습니다. 단어 하나만 입력해 주세요."*

### 5.3 [API 응답 실패 및 네트워크 오류 예외]
- **상황:** 네트워크 단절, API 서버 오류(5xx), Rate Limit 등으로 응답 생성에 실패한 경우.
- **처리 방식:**
  - 오류 발생 시 백그라운드에서 **1초 대기 후 자동으로 1회 재요청(Auto Retry)** 수행.
  - 1회 재요청까지 모두 실패할 경우:
    - 사용자가 텍스트를 다시 입력하지 않도록 **기존 입력창의 텍스트를 그대로 유지**.
    - 로딩 상태를 해제하고 결과 영역 상단에 명확한 에러 안내 문구 표시:
      > *"일시적인 오류로 인해 암기법 생성에 실패했습니다. 잠시 후 다시 시도해 주세요."*
    - '생성하기' 버튼을 다시 활성화하여 수동 재시도 허용.

### 5.4 [응답 지연 및 타임아웃 예외]
- **상황:** API 응답 지연으로 사용자가 멈춤 현상으로 인지하거나 중복 요청을 보내는 경우.
- **처리 방식:**
  - '생성하기' 버튼 클릭 즉시 버튼을 비활성화하고, 버튼 내부/결과 영역에 명확한 스피너(로딩 애니메이션) 및 안내 문구 노출 (*"뇌리에 박히는 암기 팁을 짜내는 중..."*).
  - **10초 타임아웃(Timeout) 설정:** 요청 후 10초 동안 응답이 오지 않으면 클라이언트에서 API 요청을 즉시 강제 중단(Abort).
  - 중단 후 입력값은 그대로 보존하며 다음 경고 안내 문구 노출:
    > *"요청 시간이 초과되었습니다. 다시 시도해 주세요."*
  - '생성하기' 버튼을 다시 활성화.

### 5.5 [무의미한 텍스트/알 수 없는 단어 입력 예외 (AI 가드레일)]
- **상황:** 자음/모음 남발(예: `ㅋㅋㅋㅋ`, `asdfgh`), 의미를 알 수 없는 외계어, 존재하지 않는 허구 개념을 입력한 경우.
- **처리 방식:**
  - 시스템 프롬프트 규칙에 예외 가드레일 주입:
    > *"입력된 단어가 실재하지 않거나, 뜻을 알 수 없는 무의미한 나열인 경우 절대로 지어내지(Hallucination) 말고 `{"error": "INVALID_INPUT"}` 형태로 응답하라."*
  - 프론트엔드가 `INVALID_INPUT` 응답을 수신하면 퀴즈/스토리 카드 대신 안내 카드 노출:
    > *"올바른 단어 또는 개념을 입력해 주세요. (예: 역사적 사건, 영단어, 전문 용어)"*
  - 입력창 포커스를 유지하여 사용자가 즉시 수정할 수 있도록 유도.

### 5.6 [AI JSON 파싱 오류 예외]
- **상황:** LLM 응답이 지정된 JSON 규격을 벗어나거나 퀴즈 보기가 3개가 아닌 형태로 반환된 경우.
- **처리 방식:**
  - 클라이언트 파싱 에러 발생 시 UI 크래시를 방지하고 자동 1회 재요청 실행.
  - 실패 지속 시 기존 입력값을 보존한 채 *"결과를 생성하는 중 형식이 맞지 않아 실패했습니다. 다시 생성하기를 눌러주세요."* 안내 문구 출력.

---

## 6. 완료 조건 (Definition of Done)

1. **원페이지 UI 완성:**
   - 타이틀, 단일 인풋 필드, 생성 버튼, 결과 카드(3줄 스토리 + 3지선다 퀴즈 + 다시 생성 버튼)가 단일 화면에 정상 표시된다.
2. **핵심 기능 동작:**
   - 키워드 입력 후 버튼 클릭 시 3줄 연상 암기 팁과 3지선다 퀴즈가 정상 생성된다.
   - 퀴즈 보기 클릭 시 즉각적으로 정답/오답 색상 반전 및 해설이 노출된다.
3. **예외 처리 검증 완료:**
   - 빈 값 입력 시 버튼 비활성화 및 안내 문구 작동 확인.
   - 30자 초과 방지 및 다중 단어 경고 확인.
   - 10초 타임아웃 강제 중단 및 실패 시 자동 1회 재요청 로직 정상 작동 확인.
   - 무의미한 텍스트 입력 시 가드레일 문구 노출 확인.
   - 에러 발생 시 입력창 텍스트가 초기화되지 않고 유지되는지 확인.
4. **비기능 요구사항 준수:**
   - 로그인, 결제, 별도 DB 구축 없이 프론트엔드 단독 환경에서 완전히 동작한다.